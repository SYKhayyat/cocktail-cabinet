{
  "name": "cocktail-cabinet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:browser": "node tests/browser-smoke.mjs",
    "check": "node --check src/main.js && node --check src/engine.js"
  }
}
